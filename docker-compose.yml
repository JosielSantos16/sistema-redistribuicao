services:
  mongodb:
    image: mongo:latest
    container_name: mongo_sistema
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: user_redistribuicao
      MONGO_INITDB_ROOT_PASSWORD: senha_secreta
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: